name: Deploy Jaspr to Firebase Hosting on Merge
on:
  push:
    branches:
      - main
      - uat
      - develop
    paths:
      - 'packages/tranyx_web/**'
      - 'packages/shared/**'
      - '.github/workflows/jaspr-hosting-merge.yml'

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Check for code changes
        id: filter
        uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3.0.2
        with:
          filters: |
            code:
              - 'packages/tranyx_web/**'
              - 'packages/shared/**'
              - '.github/workflows/jaspr-hosting-merge.yml'

      - name: Setup Flutter
        if: steps.filter.outputs.code == 'true'
        uses: subosito/flutter-action@f2c4f6686ca8e8d6e6d0f28410eeef506ed66aff # v2.18.0
        with:
          channel: 'stable'
          cache: true

      - name: Install workspace dependencies
        if: steps.filter.outputs.code == 'true'
        run: flutter pub get

      - name: Install Jaspr CLI
        if: steps.filter.outputs.code == 'true'
        run: |
          flutter pub global activate jaspr_cli
          echo "$HOME/.pub-cache/bin" >> $GITHUB_PATH

      - name: Generate Env Config
        if: steps.filter.outputs.code == 'true'
        run: dart run shared:generate_env
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          XENDIT_SECRET_KEY: ${{ secrets.XENDIT_SECRET_KEY }}
          IMGBB_API_KEY: ${{ secrets.IMGBB_API_KEY }}
          GEMINI_AI_API_KEY: ${{ secrets.GEMINI_AI_API_KEY }}
          SOLANA_PRIVATE_KEY: ${{ secrets.SOLANA_PRIVATE_KEY }}
          SOLANA_PUBLIC_KEY: ${{ secrets.SOLANA_PUBLIC_KEY }}
          TREASURY_PRIVATE_KEY: ${{ secrets.TREASURY_PRIVATE_KEY }}
          TREASURY_PUBLIC_KEY: ${{ secrets.TREASURY_PUBLIC_KEY }}

      - name: Build Jaspr Web
        if: steps.filter.outputs.code == 'true'
        working-directory: packages/tranyx_web
        env:
          REF_NAME: ${{ github.ref_name }}
          XENDIT_SECRET_KEY: ${{ secrets.XENDIT_SECRET_KEY }}
          IMGBB_API_KEY: ${{ secrets.IMGBB_API_KEY }}
          GEMINI_AI_API_KEY: ${{ secrets.GEMINI_AI_API_KEY }}
          SOLANA_PUBLIC_KEY: ${{ secrets.SOLANA_PUBLIC_KEY }}
        run: |
          jaspr clean
          if [[ "$REF_NAME" == "main" ]]; then
            jaspr build --dart-define=ENV=prod --dart-define=XENDIT_SECRET_KEY="$XENDIT_SECRET_KEY" --dart-define=IMGBB_API_KEY="$IMGBB_API_KEY" --dart-define=GEMINI_AI_API_KEY="$GEMINI_AI_API_KEY" --dart-define=SOLANA_PUBLIC_KEY="$SOLANA_PUBLIC_KEY"
          elif [[ "$REF_NAME" == "uat" ]]; then
            jaspr build --dart-define=ENV=uat --dart-define=XENDIT_SECRET_KEY="$XENDIT_SECRET_KEY" --dart-define=IMGBB_API_KEY="$IMGBB_API_KEY" --dart-define=GEMINI_AI_API_KEY="$GEMINI_AI_API_KEY" --dart-define=SOLANA_PUBLIC_KEY="$SOLANA_PUBLIC_KEY"
          elif [[ "$REF_NAME" == "develop" ]]; then
            jaspr build --dart-define=ENV=dev --dart-define=XENDIT_SECRET_KEY="$XENDIT_SECRET_KEY" --dart-define=IMGBB_API_KEY="$IMGBB_API_KEY" --dart-define=GEMINI_AI_API_KEY="$GEMINI_AI_API_KEY" --dart-define=SOLANA_PUBLIC_KEY="$SOLANA_PUBLIC_KEY"
          fi

      - name: Deploy to Firebase Hosting (prod)
        if: steps.filter.outputs.code == 'true' && github.ref_name == 'main'
        continue-on-error: true
        uses: FirebaseExtended/action-hosting-deploy@7c850a480ce753f4f06f010801fc5a43787740bb # v0.2.3
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TRANYX_APP }}
          channelId: live
          projectId: tranyx-app

      - name: Deploy to Firebase Hosting (uat)
        if: steps.filter.outputs.code == 'true' && github.ref_name == 'uat'
        continue-on-error: true
        uses: FirebaseExtended/action-hosting-deploy@7c850a480ce753f4f06f010801fc5a43787740bb # v0.2.3
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TRANYX_UAT }}
          projectId: tranyx-uat
          channelId: live

      - name: Deploy to Firebase Hosting (dev)
        if: steps.filter.outputs.code == 'true' && github.ref_name == 'develop'
        continue-on-error: true
        uses: FirebaseExtended/action-hosting-deploy@7c850a480ce753f4f06f010801fc5a43787740bb # v0.2.3
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TRANYX_DEV }}
          projectId: tranyx-dev
          channelId: live